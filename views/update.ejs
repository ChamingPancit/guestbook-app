<%- include("partials/header.ejs") %>

    </p>
    <div class="flex items-center justify-center mt-20">
        <form action="/update/<%= entry.id %>" method="POST" class="flex flex-col">
            <label class="mb-2 text-center text-2xl font-bold" for="updatedGuest">Update Name</label>
            <input class="mb-8 text-center p-4 border-2 border-gray-800 rounded-md" type="text" name="updatedGuest"
                placeholder="Update your name" value="<%= entry.name %>">
            <label class="text-center mb-2 text-2xl font-bold" for="updatedMessage">Update Message</label>
            <input class="mb-10 p-10 text-center border-2 border-gray-800 rounded-md" type="text" name="updatedMessage"
                placeholder="Update your message" value="<%= entry.message %>">
            <button
                class="relative overflow-hidden border-2 border-blue-700 rounded-md text-blue-700 uppercase no-underline px-5 py-2 text-lg cursor-pointer font-bold bg-transparent transition-all duration-1000 btn-tailwind"
                type="submit">Submit</button>
        </form>
    </div>
    <%- include("partials/footer.ejs") %>